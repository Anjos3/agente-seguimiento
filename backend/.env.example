# =====================================================
# CONFIGURACIÓN DEL SERVIDOR
# =====================================================

# Puerto donde correrá el servidor (default: 3000)
PORT=3000

# Host del servidor (0.0.0.0 = acepta conexiones externas)
HOST=0.0.0.0

# Entorno de ejecución (development | production)
NODE_ENV=development


# =====================================================
# BASE DE DATOS (PostgreSQL)
# =====================================================

# URL de conexión completa a PostgreSQL
# Formato: postgresql://usuario:contraseña@host:puerto/nombre_db
DATABASE_URL=postgresql://postgres:tu_contraseña@localhost:5432/task_tracker_ai

# O puedes usar variables separadas:
# DB_HOST=localhost
# DB_PORT=5432
# DB_USER=postgres
# DB_PASSWORD=tu_contraseña
# DB_NAME=task_tracker_ai


# =====================================================
# AUTENTICACIÓN (JWT)
# =====================================================

# Secreto para firmar los tokens JWT (CAMBIAR en producción)
# Genera uno seguro con: openssl rand -hex 32
JWT_SECRET=tu_secreto_super_seguro_cambiar_en_produccion

# Tiempo de expiración del token (ej: 7d = 7 días, 24h = 24 horas)
JWT_EXPIRES_IN=7d


# =====================================================
# OPENAI (GPT-5)
# =====================================================

# Tu API key de OpenAI
# Obtenerla en: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-tu-api-key-de-openai

# Modelo a usar (gpt-5-mini para uso diario, gpt-5 para análisis complejos)
OPENAI_MODEL=gpt-4o-mini


# =====================================================
# DEEPGRAM (Voice-to-Text)
# =====================================================

# Tu API key de Deepgram
# Obtenerla en: https://console.deepgram.com/
DEEPGRAM_API_KEY=tu-api-key-de-deepgram

# Modelo de Deepgram a usar
DEEPGRAM_MODEL=nova-2


# =====================================================
# CORS (Orígenes permitidos)
# =====================================================

# URLs permitidas para hacer requests (separadas por coma)
# En desarrollo: *
# En producción: https://tu-dominio.com
CORS_ORIGIN=*
